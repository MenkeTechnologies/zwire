diff --git a/chrome/browser/search/search.cc b/chrome/browser/search/search.cc
index 398e67f9cb..8eda8a2fe5 100644
--- a/chrome/browser/search/search.cc
+++ b/chrome/browser/search/search.cc
@@ -14,6 +14,7 @@
 #include "base/metrics/histogram_macros.h"
 #include "build/build_config.h"
 #include "chrome/browser/profiles/profile.h"
+#include "extensions/browser/extension_registry.h"
 #include "chrome/browser/search_engines/template_url_service_factory.h"
 #include "chrome/browser/search_engines/ui_thread_search_terms_data.h"
 #include "chrome/browser/supervised_user/supervised_user_url_filtering_service_factory.h"
@@ -373,16 +374,32 @@ std::optional<GURL> GetEffectiveURLForInstant(const GURL& url,
 
 bool HandleNewTabURLRewrite(GURL* url,
                             content::BrowserContext* browser_context) {
-  if (!IsInstantExtendedAPIEnabled()) {
+  if (!(url->SchemeIs(content::kChromeUIScheme) &&
+        (url->GetHost() == chrome::kChromeUINewTabHost ||
+         url->GetHost() == chrome::kChromeUINewTabPageHost))) {
     return false;
   }
 
-  if (!(url->SchemeIs(content::kChromeUIScheme) &&
-        url->GetHost() == chrome::kChromeUINewTabHost)) {
+  Profile* profile = Profile::FromBrowserContext(browser_context);
+
+  // zbrowser: the New Tab page is ALWAYS the zwire newtab extension — never the
+  // default WebUI/Google NTP. Redirect BOTH chrome://newtab and
+  // chrome://new-tab-page UNCONDITIONALLY (don't gate on an extension-registry
+  // snapshot: at startup it races the extension's registration, which leaked the
+  // Google NTP into the first/restored tab). The ID is pinned by the newtab
+  // extension's manifest key and its newtab.html is web-accessible; the
+  // reverse-rewrite keeps the omnibox showing chrome://newtab. Skip incognito,
+  // where the extension may not run.
+  if (profile && !profile->IsOffTheRecord()) {
+    *url = GURL(
+        "chrome-extension://gpoepnekoiplhkegjpocnpeijiefgieb/newtab.html");
+    return true;
+  }
+
+  if (!IsInstantExtendedAPIEnabled()) {
     return false;
   }
 
-  Profile* profile = Profile::FromBrowserContext(browser_context);
   NewTabURLDetails details(NewTabURLDetails::ForProfile(profile));
   UMA_HISTOGRAM_ENUMERATION("NewTabPage.URLState", details.state,
                             NEW_TAB_URL_MAX);
@@ -395,6 +412,14 @@ bool HandleNewTabURLRewrite(GURL* url,
 
 bool HandleNewTabURLReverseRewrite(GURL* url,
                                    content::BrowserContext* browser_context) {
+  // zbrowser: keep the omnibox clean (chrome://newtab) for the forced zwire
+  // newtab extension page (see HandleNewTabURLRewrite).
+  if (url->SchemeIs("chrome-extension") &&
+      url->GetHost() == "gpoepnekoiplhkegjpocnpeijiefgieb") {
+    *url = chrome::ChromeUINewTabURLAsGURL();
+    return true;
+  }
+
   if (!IsInstantExtendedAPIEnabled()) {
     return false;
   }
