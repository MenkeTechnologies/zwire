diff --git a/chrome/browser/ui/views/location_bar/location_bar_view.cc b/chrome/browser/ui/views/location_bar/location_bar_view.cc
--- a/chrome/browser/ui/views/location_bar/location_bar_view.cc
+++ b/chrome/browser/ui/views/location_bar/location_bar_view.cc
@@ -627,8 +627,11 @@
 
 // static
 int LocationBarView::ComputeBorderRadius(gfx::Size size) {
-  return ChromeLayoutProvider::Get()->GetCornerRadiusMetric(
-      views::Emphasis::kMaximum, size);
+  // zwire HUD: sharp 2px omnibox field, matching the strykelang HUD
+  // radius (.omni border-radius: 2px), instead of the fully-rounded pill.
+  return std::min(ChromeLayoutProvider::Get()->GetCornerRadiusMetric(
+                      views::Emphasis::kMaximum, size),
+                  2);
 }
 
 // static
